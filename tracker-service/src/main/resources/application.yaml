spring:
  main:
    web-application-type: none
  application:
    name: tracker-service
  redis:
    client-name: tracker-service
    host: localhost

redis:
  imei-set-name: ImeiAllowed

tcp:
  server:
    coban-port: 9000